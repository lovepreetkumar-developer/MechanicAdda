package com.techfathers.mechanic_adda.util

class ExtraCode {

    /*<!--<com.airbnb.lottie.LottieAnimationView
    android:id="@+id/lottie_animation"
    android:layout_width="@dimen/_100sdp"
    android:layout_height="@dimen/_100sdp"
    android:layout_centerInParent="true"
    app:lottie_autoPlay="true"
    app:lottie_loop="true"
    app:lottie_rawRes="@raw/cute_tiger" />-->

    //activity?.showStatusBar()

    val adminModel = AdminModel()
        getDatabaseReference().child(BuildConfig.SECRET_CODE)
            .child(BuildConfig.ADMIN)
            .setValue(adminModel).addOnCompleteListener(requireActivity()) { task ->
                if (task.isSuccessful){
                    Timber.d("")
                }
            }

    */
}